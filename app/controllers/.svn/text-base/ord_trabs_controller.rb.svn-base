class OrdTrabsController < ApplicationController

  hobo_model_controller

  auto_actions :all
  # , :except => :index
  auto_actions_for :producto, :create
  
  index_action :tablero do
    @todas = OrdTrab.find(:all)
  end

end
